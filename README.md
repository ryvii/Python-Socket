# Python TCP & UDP Client/Server

This project implements TCP and UDP client/server in Python.

- TCP supports multiple clients using threads.
- UDP is connectionless and simple to test.

## How to Run

1. Run `server.py` first.
2. Run `client.py`.
